{
  "permissions": {
    "allow": [
      "Bash(python3 -c ' *)",
      "PowerShell(Get-WinEvent -LogName Application -MaxEvents 50 | Where-Object { $_.ProviderName -match 'TradeWiser|Python' -or $_.Message -match 'TradeWiser' } | Select-Object -First 10 TimeCreated, LevelDisplayName, Message | Format-List)"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "shell": "powershell",
            "command": "& '${CLAUDE_PROJECT_DIR}\\.claude\\hooks\\sync-on-edit.ps1'",
            "timeout": 60,
            "statusMessage": "Syncing to installed service..."
          }
        ]
      }
    ]
  }
}
